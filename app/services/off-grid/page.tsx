import PagePlaceholder from '../../components/page-placeholder';

export default function OffGridServicePage() {
  return (
    <PagePlaceholder
      title="Off-grid solar systems"
      description="Standalone power solutions for remote, rural and backup-critical environments where grid access is limited or inconsistent."
      ctaLabel="Back to home"
      ctaHref="/"
    />
  );
}
